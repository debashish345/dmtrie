import { HighlightInlineCodePipePipe } from './highlight-inline-code-pipe.pipe';

describe('HighlightInlineCodePipePipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightInlineCodePipePipe();
    expect(pipe).toBeTruthy();
  });
});
