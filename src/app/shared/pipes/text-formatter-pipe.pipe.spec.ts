import { TextFormatterPipePipe } from './text-formatter-pipe.pipe';

describe('TextFormatterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TextFormatterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
